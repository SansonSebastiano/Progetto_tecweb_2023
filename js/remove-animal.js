import { ref, defaultStorage} from "./init-db";

/*
function deleteFile(url) {
    const fileRef = ref(defaultStorage, url);

    deletObject(fileRef).then(() => {
        console.log(url + " deleted");
    }).catch((error) => {
        console.log(error);
    });
}
*/